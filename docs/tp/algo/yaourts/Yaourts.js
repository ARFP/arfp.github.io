class Yaourts
{
    /**
     * Retourne les 2 couleurs les plus populaires à partir d'un tableau de données
     * @param array colors Un tableau indexé où chaque élément correspond à la couleur choisie par une personne interrogée.
     * Exemple : ['rouge', 'jaune', 'bleu', 'jaune', 'rouge', 'jaune'].
     * @return string Les 2 couleurs les plus populaires. La couleur la plus populaire apparait en premier.
     */
    run(colors)
    {

        // Votre code ici !

    }
}
